	var x = 3;   // reads the values
	var y = 5;
	var z = 7;

	if (x > y > z) {                                      //condition for greatest number
		console.log("Geatest number is " + x);
	} else if (y > z > x) {
		console.log("Geatest number is " +y);
	} else {
		console.log("Geatest number is " +z);
	}